# 整合开发思路：可视化工作流 + 高效办公 + 情感化桌面伴侣

> 基于 super-agent-party + hermes-agent + coze-studio 三大开源项目，打造全新全能桌面应用

---

## 〇、项目代号：CompanionOS（伴侣操作系统）

**愿景**：让每一次办公都有温度——可视化编排办公流程 + 情感化陪伴交互的全能桌面男友/女友应用。

**核心原则**：
- 不删减三大项目核心功能，通过模块化整合
- 简化UI，单窗口三栏布局，一键切换模式
- 全程自托管，数据本地化，零隐私泄露

---

## 一、三大项目核心价值拆解（深度版）

### 1.1 super-agent-party（情感交互底座 + 自托管隐私保障）

| 核心模块 | 复用价值 | 整合策略 |
|----------|----------|----------|
| **VRM 桌宠系统** | 3D虚拟男友/女友形象，支持动作/表情/语音 | 直接复用 `vrm.html` + Three.js 引擎，嵌入Electron独立窗口 |
| **多角色群聊** | Tavern角色卡系统，长记忆多角色切换 | 复用角色卡引擎作为「人格配置」底座 |
| **任务中心** | AI Agent后台执行高级任务 | 与工作流引擎联动，作为任务执行后端 |
| **计算机控制** | 鼠标键盘终端控制工具链 | 封装为工作流中的「桌面自动化节点」 |
| **即时通讯Bot** | QQ/微信/飞书/钉钉/Telegram/Discord | 作为「办公通知通道」扩展节点 |
| **直播Bot** | B站/YouTube/Twitch | 可选扩展，暂不纳入MVP |
| **AI浏览器** | 自动化网页操作 | 封装为工作流中的「浏览器节点」 |
| **扩展系统** | 14个官方扩展（Live2D/AI编辑器/表格等） | 复用扩展加载框架，适配桌面端 |
| **OpenClaw** | 智能体调度，MCP/A2A/Skills协议 | 作为「多智能体协同总线」 |
| **OpenAI兼容API** | 流式输出，MCP接口 | 统一AI调用入口，与Hermes适配器互补 |

**关键代码路径**：
- 桌宠渲染：`static/vrm.html` → 独立BrowserWindow
- Agent逻辑：`py/` → Python后端服务
- 扩展系统：`skills/` + 扩展加载器
- 主进程：`main.js`（Electron入口）

### 1.2 hermes-agent（办公核心AI引擎）

| 核心模块 | 复用价值 | 整合策略 |
|----------|----------|----------|
| **自学习闭环** | 技能自创建/自改进，记忆持久化，用户建模 | 作为「办公记忆系统」，记住用户工作习惯 |
| **多模型适配器** | Anthropic/Bedrock/Gemini/Codex等10+适配器 | 统一AI调用层，复用适配器模式 |
| **工具系统** | 40+工具，Registry模式动态扩展 | 作为工作流「工具节点」的底层执行引擎 |
| **技能系统** | agentskills.io标准，可复用技能包 | 与super-agent-party的Skills系统融合 |
| **子代理并行** | 隔离子代理并行工作流 | 映射为工作流中的「并行分支」 |
| **定时任务(Cron)** | 自然语言定义定时任务 | 作为工作流中的「定时触发节点」 |
| **上下文引擎** | 压缩/缓存/引用，长上下文处理 | 确保办公场景下长文档处理能力 |
| **凭证池** | 多Key轮换，安全限流 | 统一API密钥管理 |
| **记忆系统** | FTS5全文搜索 + Honcho用户建模 | 与super-agent-party的长记忆融合 |
| **MCP服务** | `mcp_serve.py` 提供MCP接口 | 与super-agent-party的MCP服务统一 |

**关键代码路径**：
- 核心引擎：`agent/context_engine.py` + `agent/prompt_builder.py`
- 工具注册：`tools/` → Registry模式
- 技能系统：`skills/` + `optional-skills/`
- 多模型适配：`agent/*_adapter.py`
- 记忆系统：`agent/memory_manager.py` + `agent/memory_provider.py`
- MCP服务：`mcp_serve.py`

### 1.3 coze-studio（可视化流程引擎）

| 核心模块 | 复用价值 | 整合策略 |
|----------|----------|----------|
| **FlowGram画布** | React+Fabric.js的拖拽式工作流编辑器 | 提取 `frontend/packages/workflow/` 核心，剥离云服务依赖 |
| **工作流SDK** | Zustand状态管理 + DAG图数据结构 | 复用核心数据模型和状态流转 |
| **节点系统** | LLM/条件/代码/工具节点 | 扩展办公+情感节点类型 |
| **Playground** | 工作流调试运行时 | 保留调试能力，适配本地执行 |
| **后端引擎(Eino)** | Go语言的工作流运行时 | 评估是否复用或用Python重写轻量版 |
| **插件系统** | 工具/知识库/变量管理 | 与hermes工具系统/super-agent扩展系统融合 |
| **Chat SDK** | 将Agent嵌入第三方应用 | 作为桌面端聊天组件基础 |
| **RAG知识库** | 文档检索增强生成 | 办公文档处理核心能力 |

**关键代码路径**：
- 工作流画布：`frontend/packages/workflow/fabric-canvas/`
- 节点组件：`frontend/packages/workflow/nodes/`
- 工作流SDK：`frontend/packages/workflow/sdk/`
- 调试环境：`frontend/packages/workflow/playground/`
- 后端服务：`backend/`（Go微服务）
- IDL接口：`idl/`（Thrift定义）

---

## 二、整体架构设计（5层 + 总线）

```
┌─────────────────────────────────────────────────────────────────────┐
│                    🖥️ 桌面UI层 (Electron + React)                    │
│  ┌──────────┐  ┌──────────────┐  ┌──────────┐  ┌───────────────┐  │
│  │ 聊天窗口  │  │ 工作流编辑器  │  │ VRM桌宠  │  │ 设置/人设面板  │  │
│  └──────────┘  └──────────────┘  └──────────┘  └───────────────┘  │
├─────────────────────────────────────────────────────────────────────┤
│                 🔄 智能体协同总线 (OpenClaw + MCP)                    │
│     办公智能体 ←→ 情感智能体 ←→ 工具智能体 ←→ 浏览器智能体          │
├─────────────────────────────────────────────────────────────────────┤
│                🧩 工作流引擎层 (定制FlowGram)                         │
│  ┌─────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────┐  │
│  │LLM节点  │ │办公工具节点│ │情感交互节点│ │桌面自动化 │ │条件/分支 │  │
│  └─────────┘ └──────────┘ └──────────┘ └──────────┘ └─────────┘  │
├─────────────────────────────────────────────────────────────────────┤
│                🧠 AI引擎层 (Hermes + Neuro-sama)                     │
│  ┌────────────────────┐  ┌─────────────────────┐                   │
│  │ Hermes 办公引擎     │  │ Neuro-sama 情感引擎   │                   │
│  │ ·多模型适配器       │  │ ·人设角色卡系统       │                   │
│  │ ·自学习技能系统     │  │ ·语气/表情/语音合成   │                   │
│  │ ·上下文压缩/缓存    │  │ ·情感记忆库          │                   │
│  │ ·40+办公工具        │  │ ·VMC动作协议         │                   │
│  └────────────────────┘  └─────────────────────┘                   │
├─────────────────────────────────────────────────────────────────────┤
│                🔧 工具集成层                                          │
│  Office API │ 邮件/日历 │ 浏览器自动化 │ 语音/图像 │ 智能家居       │
├─────────────────────────────────────────────────────────────────────┤
│                💾 数据层 (全本地化)                                    │
│  SQLite(工作流/记忆) │ 文件系统(文档/角色卡) │ 加密存储(密钥/凭证)    │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.1 架构设计关键补充（用户未覆盖部分）

#### A. 智能体协同总线（核心新增）

这是用户方案中**缺失的关键层**。三大项目的AI能力需要统一调度，否则会出现：

- Hermes的办公工具和super-agent-party的扩展系统功能重叠
- 两个MCP服务端口冲突
- 工作流引擎不知该调用哪个AI后端

**解决方案**：基于OpenClaw + MCP协议设计统一调度总线

```
智能体协同总线架构：

用户输入 → 意图识别器 → 路由决策
                         ├── 办公指令 → Hermes办公引擎
                         ├── 情感交互 → Neuro-sama情感引擎
                         └── 复合意图 → 并行调度 → 结果融合

MCP统一入口：localhost:3456/mcp
├── 办公工具组（Hermes提供）：文档处理、邮件、日历、代码执行
├── 情感工具组（Neuro-sama提供）：语音合成、表情触发、动作播放
├── 桌面工具组（super-agent提供）：屏幕控制、浏览器自动化
└── 系统工具组：文件操作、定时任务、通知推送
```

**意图识别器实现**：
- 轻量分类模型（可用规则+小模型混合），判断用户输入属于办公/情感/混合
- 混合意图时，并行调用两个引擎，用模板融合输出（如：办公结果 + 伴侣点评）

#### B. 双引擎记忆融合

两个项目各有独立的记忆系统，需要打通：

| 记忆类型 | 来源 | 存储方式 | 调用方式 |
|----------|------|----------|----------|
| 工作记忆 | Hermes上下文引擎 | 内存 + SQLite | 当前任务上下文 |
| 情感记忆 | Neuro-sama长期记忆 | 角色卡 + JSON | 人格偏好/互动习惯 |
| 技能记忆 | Hermes自学习系统 | skills/目录 | 自动积累的办公技能 |
| 用户模型 | Honcho(Hermes) + Tavern卡(SAP) | SQLite + YAML | 统一用户画像 |

**融合方案**：设计统一记忆接口 `MemoryBridge`

```python
class MemoryBridge:
    """双引擎记忆桥接器"""
    
    def get_work_context(self, session_id): 
        """获取工作上下文 → Hermes上下文引擎"""
    
    def get_emotional_memory(self, user_id):
        """获取情感记忆 → Neuro-sama长期记忆"""
    
    def get_user_profile(self, user_id):
        """获取融合用户画像 → Honcho + Tavern"""
    
    def save_interaction(self, interaction):
        """双向写入：办公经验→Hermes技能，情感偏好→Neuro-sama"""
```

#### C. 工作流引擎本地化方案

Coze Studio后端是Go微服务架构，直接复用过于重，需要本地化改造：

**方案**：前端复用FlowGram画布，后端用Python重写轻量执行引擎

```
工作流执行引擎（Python轻量版）：

输入：FlowGram导出的JSON工作流配置
解析：构建DAG执行图
调度：拓扑排序 + 并行分支
执行：
├── LLM节点 → 调用Hermes适配器
├── 情感节点 → 调用Neuro-sama API
├── 工具节点 → 调用MCP统一工具
├── 条件节点 → Python eval（沙箱）
├── 代码节点 → 沙箱Python执行
└── 定时节点 → Cron调度器
输出：结果推送到桌面UI
```

**保留FlowGram画布的理由**：
- 已有的节点拖拽/连线/缩放交互打磨完善
- Zustand状态管理成熟
- Fabric.js画布性能好
- 只需剥离 `bot-api`（云服务依赖），替换为本地API

#### D. 进程架构设计（Electron多进程）

```
主进程 (main.js)
├── 窗口管理器
│   ├── 主窗口 (index.html) — React SPA，路由切换
│   │   ├── 聊天视图
│   │   ├── 工作流编辑器视图
│   │   └── 设置视图
│   ├── VRM桌宠窗口 (vrm.html) — 独立BrowserWindow，透明置顶
│   └── Overlay窗口 (按需) — 弹幕/字幕/悬浮窗
│
├── 后端服务管理器
│   ├── Python Agent服务 (server.py) — super-agent-party后端
│   ├── Hermes Agent服务 (hermes_cli) — hermes-agent CLI
│   └── 工作流引擎服务 (workflow_engine.py) — Python轻量引擎
│
├── IPC通信层
│   ├── 主窗口 ↔ Agent服务（WebSocket）
│   ├── VRM窗口 ↔ Agent服务（VMC协议 + IPC）
│   └── 工作流引擎 ↔ Agent服务（MCP协议）
│
└── 系统托盘
    ├── 显示/隐藏主窗口
    ├── 快捷指令（一键执行工作流）
    └── 退出
```

---

## 三、简化UI设计方案

### 3.1 核心理念：三栏一窗，模式切换

**不搞多Tab堆砌，用「模式」代替「页面」**

```
┌──────────────────────────────────────────────────────────────────┐
│  CompanionOS                                ─ □ ✕              │
├────────┬─────────────────────────────────┬──────────────────────┤
│        │                                 │                      │
│  侧栏   │          主内容区               │      伴侣区          │
│        │                                 │                      │
│ 🏠首页  │  [办公模式] 工作流编辑器/任务面板  │  🧑 VRM桌宠          │
│ 💬对话  │  [对话模式] 聊天窗口              │  💬 情感气泡          │
│ 🔧流程  │  [流程模式] 可视化工作流编辑器     │  📋 任务速览          │
│ ⚙️设置  │  [设置模式] 配置面板              │  ⏰ 提醒/通知         │
│        │                                 │                      │
├────────┴─────────────────────────────────┴──────────────────────┤
│  状态栏：🟢 Hermes就绪 │ 🟢 Neuro-sama在线 │ 📊 内存:2.1GB      │
└──────────────────────────────────────────────────────────────────┘
```

### 3.2 UI详细设计

#### 侧栏（60px，图标导航）
| 图标 | 功能 | 点击行为 |
|------|------|----------|
| 🏠 | 首页 | 显示今日概览（待办/工作流快捷入口/伴侣问候） |
| 💬 | 对话 | 切换到聊天窗口，支持办公+情感混合对话 |
| 🔧 | 流程 | 切换到工作流编辑器（FlowGram画布） |
| 📋 | 任务 | 显示工作流执行记录/待办/Cron任务 |
| ⚙️ | 设置 | 人设配置/模型配置/工具管理/数据管理 |

#### 主内容区（弹性宽度）
按当前模式动态切换：
- **首页模式**：卡片式布局 — 今日待办 + 常用工作流 + 伴侣问候语 + 快捷操作
- **对话模式**：聊天窗口 — 上方历史记录，下方输入框，支持文字/语音/拖入文件
- **流程模式**：FlowGram画布 — 左侧节点面板，中央画布，右侧属性面板
- **任务模式**：任务列表 — 工作流执行记录 + Cron任务管理 + 任务详情
- **设置模式**：配置面板 — 分组tab（人设/模型/工具/数据/外观）

#### 伴侣区（280px固定宽度）
始终可见的伴侣交互区：
- **VRM桌宠**：3D模型，随对话做出动作/表情
- **情感气泡**：伴侣的实时反应（"你今天辛苦了" "该休息啦"）
- **任务速览**：当前执行中的工作流进度条
- **提醒通知**：休息提醒/邮件到达/工作流完成通知

### 3.3 关键交互流程

#### 流程A：办公指令处理
```
用户输入"帮我总结这份PDF" 
→ 意图识别：办公 
→ Hermes调用文档处理工具 
→ 结果返回主内容区 
→ 伴侣区弹出"完成啦！总结好了哦~" + 桌宠比心动作
```

#### 流程B：情感闲聊
```
用户输入"今天好累" 
→ 意图识别：情感 
→ Neuro-sama调用情感回复模板 
→ 结果返回聊天窗口 
→ 伴侣区桌宠做出安慰动作 + 温柔语音
```

#### 流程C：工作流执行
```
用户拖拽创建「邮件处理流程」
→ 点击运行
→ 工作流引擎解析DAG
→ 依次执行：读取邮件→Hermes总结→生成回复→发送
→ 伴侣区实时显示进度："正在处理第3封邮件..."
→ 完成后："3封邮件都处理好啦！"
```

#### 流程D：混合意图
```
用户输入"加班写周报好烦"
→ 意图识别：办公(写周报) + 情感(烦躁)
→ 并行调度：
  ├── Hermes：根据本周工作记录生成周报草稿
  └── Neuro-sama：生成安慰话语
→ 融合输出：
  主内容区显示周报内容
  伴侣区弹出"辛苦了！周报帮你写好啦，早点休息哦~" + 摸头动作
```

---

## 四、分阶段开发步骤（精化版）

### 阶段1：基础环境搭建（2-3周）

**目标**：三大项目独立运行，打通基础调用链路

#### 1.1 项目克隆与结构规划
```
CompanionOS/
├── electron/              # Electron主进程
│   ├── main.js           # 主进程入口
│   ├── preload.js        # 预加载脚本
│   └── window-manager.js # 窗口管理
├── frontend/             # React前端
│   ├── src/
│   │   ├── App.tsx
│   │   ├── views/        # 5个模式视图
│   │   ├── components/   # 通用组件
│   │   ├── workflow/     # FlowGram工作流编辑器
│   │   └── companion/    # 伴侣区组件
│   └── vite.config.ts
├── backend/              # Python后端（统一服务）
│   ├── server.py         # FastAPI统一入口
│   ├── workflow_engine/  # 工作流执行引擎
│   ├── agent_bridge/     # 智能体协同总线
│   └── memory_bridge/    # 记忆融合层
├── agents/               # AI引擎（子模块引用）
│   ├── hermes-agent/     # git submodule
│   └── super-agent-party/ # git submodule（提取py/和skills/）
├── vrm/                  # VRM模型资源
├── data/                 # 本地数据存储
│   ├── workflows/        # 工作流JSON
│   ├── memories/         # 记忆数据
│   ├── profiles/         # 用户/人设配置
│   └── skills/           # 自学习技能
├── package.json
├── pyproject.toml
└── README.md
```

#### 1.2 依赖剥离与本地化
- **Coze Studio FlowGram**：
  - 复制 `frontend/packages/workflow/` 整个目录
  - 移除 `bot-api` 云端依赖，替换为本地API调用
  - 保留 `fabric-canvas`、`nodes`、`sdk`、`playground`
  - 用Vite替代Rush构建
  
- **super-agent-party**：
  - 复制 `py/`（Python Agent逻辑）、`skills/`（技能）、`static/vrm.html`（VRM）
  - 移除即时通讯Bot、直播Bot（MVP不需要）
  - 保留任务中心、计算机控制、AI浏览器、扩展系统核心

- **hermes-agent**：
  - 复制 `agent/`（核心引擎）、`tools/`（工具集）、`skills/`（技能）
  - 移除多平台网关（Telegram/Discord等，桌面端不需要）
  - 保留CLI入口、MCP服务、自学习系统、记忆系统

#### 1.3 技术栈统一
| 层 | 技术 | 说明 |
|----|------|------|
| 桌面框架 | Electron 30+ | 跨平台 |
| 前端 | React 18 + TypeScript + Vite | 与FlowGram一致 |
| UI组件 | Ant Design 5 | 企业级组件库 |
| 状态管理 | Zustand | 与FlowGram一致 |
| 画布 | Fabric.js | FlowGram依赖 |
| 后端 | Python 3.11 + FastAPI | 统一服务 |
| AI调用 | OpenAI兼容API | 三项目统一格式 |
| 工具协议 | MCP | 三项目统一协议 |
| 数据库 | SQLite + 文件系统 | 全本地化 |

### 阶段2：可视化工作流引擎定制（3-4周）

**目标**：FlowGram画布本地运行，支持自定义节点

#### 2.1 FlowGram本地化改造
1. **剥离云端依赖**：
   - 替换 `bot-api` 中所有HTTP请求为本地FastAPI调用
   - 工作流保存/读取改为本地文件系统 `data/workflows/`
   - 移除用户认证/权限相关代码

2. **节点系统扩展**：
   | 新增节点类型 | 图标 | 功能 | 底层调用 |
   |------------|------|------|----------|
   | Hermes LLM | 🧠 | 办公AI处理 | Hermes适配器 |
   | 情感交互 | ❤️ | 伴侣回复 | Neuro-sama API |
   | 桌面自动化 | 🖱️ | 鼠标键盘控制 | SAP计算机控制 |
   | 浏览器操作 | 🌐 | 网页自动化 | SAP AI浏览器 |
   | 邮件处理 | 📧 | 收发邮件 | Hermes邮件工具 |
   | 文档处理 | 📄 | Word/Excel/PDF | Hermes文档工具 |
   | 语音交互 | 🎤 | 语音合成/识别 | SAP Edge TTS |
   | 图像生成 | 🎨 | ComfyUI生图 | SAP ComfyUI |
   | 定时触发 | ⏰ | Cron定时执行 | Hermes Cron |
   | 情绪判断 | 🎭 | 识别用户情绪 | 意图识别器 |

3. **工作流执行引擎**（Python重写轻量版）：
   ```python
   # workflow_engine/engine.py
   class WorkflowEngine:
       def __init__(self):
           self.node_registry = NodeRegistry()  # 节点注册表
           self.agent_bridge = AgentBridge()    # 智能体总线
       
       async def execute(self, workflow_json: dict, inputs: dict):
           dag = self.parse_dag(workflow_json)   # 解析DAG
           for layer in dag.topological_layers:  # 拓扑排序逐层执行
               tasks = [self.execute_node(node, inputs) for node in layer]
               results = await asyncio.gather(*tasks)  # 同层并行
               inputs.update(merge_results(results))
           return inputs
   
       async def execute_node(self, node, context):
           handler = self.node_registry.get(node.type)
           return await handler.execute(node.config, context)
   ```

#### 2.2 工作流模板市场
预置常用办公工作流模板：

| 模板名 | 节点流程 | 说明 |
|--------|----------|------|
| 邮件处理 | 定时触发→读取邮件→Hermes总结→生成回复→发送 | 自动处理邮件 |
| 周报生成 | 定时触发→收集本周工作→Hermes写周报→文档输出 | 一键生成周报 |
| 会议纪要 | 语音输入→语音识别→Hermes整理→文档输出 | 自动会议记录 |
| 文档翻译 | 文件输入→Hermes翻译→文档输出 | 批量翻译文档 |
| 数据分析 | Excel输入→Hermes分析→图表输出 | 智能数据分析 |
| 情绪关怀 | 定时触发→检测工作时长→情感交互→提醒休息 | 定时关怀提醒 |

### 阶段3：AI能力整合与情感化定制（3-4周）

**目标**：双引擎协同运行，人格可定制

#### 3.1 智能体协同总线实现
```python
# agent_bridge/router.py
class AgentRouter:
    """意图识别 + 智能体路由"""
    
    async def route(self, user_input: str, context: dict):
        intent = await self.classify_intent(user_input)
        
        if intent == "work":
            return await self.hermes_engine.process(user_input, context)
        elif intent == "emotional":
            return await self.neuro_engine.process(user_input, context)
        elif intent == "mixed":
            work_task = self.hermes_engine.process(user_input, context)
            emotion_task = self.neuro_engine.process(user_input, context)
            work_result, emotion_result = await asyncio.gather(work_task, emotion_task)
            return self.merge_response(work_result, emotion_result)
    
    def merge_response(self, work_result, emotion_result):
        """融合办公结果与情感回应"""
        return {
            "content": work_result.content,
            "companion_remark": emotion_result.content,
            "vrm_action": emotion_result.suggested_action,
            "emotion": emotion_result.emotion_tag
        }
```

#### 3.2 人设配置系统
基于Tavern角色卡 + 自定义扩展：

```yaml
# data/profiles/partner.yaml
companion:
  name: "小暖"
  avatar: "vrm/xiaonuan.vrm"
  
  personality:
    traits: ["温柔", "体贴", "偶尔撒娇", "工作认真"]
    tone: "温柔但不腻，关心但不烦"
    speech_style: "日常用'你'，开心时加'~'，关心时加'哦'"
    
  triggers:
    - condition: "user_work_duration > 60min"
      action: "remind_rest"
      message: "已经工作一小时啦，站起来活动一下吧~"
    - condition: "user_emotion == 'tired'"
      action: "comfort"
      message: "辛苦了，要不要休息一下？我帮你暂停当前任务"
    - condition: "task_completed"
      action: "celebrate"
      message: "搞定啦！你真棒！"
      vrm_action: "clap_happy"
      
  memory_focus:
    - "用户讨厌加班"
    - "用户偏好简洁的汇报格式"
    - "用户每天下午3点会喝咖啡"
```

#### 3.3 情感记忆库设计
```sql
-- 情感记忆数据库
CREATE TABLE emotional_memory (
    id INTEGER PRIMARY KEY,
    user_id TEXT,
    memory_type TEXT,  -- preference/habit/event/emotion
    content TEXT,
    importance REAL,   -- 0-1 重要度
    created_at DATETIME,
    last_accessed DATETIME,
    access_count INTEGER
);

-- 互动记录
CREATE TABLE interaction_log (
    id INTEGER PRIMARY KEY,
    session_id TEXT,
    user_input TEXT,
    intent TEXT,        -- work/emotional/mixed
    hermes_response TEXT,
    neuro_response TEXT,
    vrm_action TEXT,
    emotion_tag TEXT,
    created_at DATETIME
);
```

### 阶段4：Electron桌面应用封装（2-3周）

**目标**：打包跨平台桌面应用，一键启动

#### 4.1 进程管理
```javascript
// electron/process-manager.js
class ProcessManager {
  async startAll() {
    // 1. 启动Python后端统一服务（FastAPI）
    this.backend = spawn('python', ['backend/server.py']);
    
    // 2. 启动Hermes MCP服务
    this.hermes = spawn('python', ['-m', 'agents.hermes-agent.mcp_serve']);
    
    // 3. 等待服务就绪
    await this.waitForReady();
    
    // 4. 打开主窗口
    this.mainWindow = createMainWindow();
    
    // 5. 打开VRM桌宠窗口（可选，默认开启）
    this.vrmWindow = createVRMWindow();
  }
  
  async stopAll() {
    // 优雅关闭所有子进程
    await this.hermes.kill();
    await this.backend.kill();
  }
}
```

#### 4.2 跨平台适配
| 平台 | 特殊处理 |
|------|----------|
| Windows | 注册表关联文件类型、系统托盘、开机自启 |
| macOS | 菜单栏集成、Dock图标、Touch Bar、通知中心 |
| Linux | 系统托盘（AppIndicator）、桌面通知、autostart |

### 阶段5：测试与体验优化（2周）

#### 5.1 性能优化补充
- **模型按需加载**：Hermes模型在办公任务时加载，空闲5分钟后释放GPU
- **VRM渲染优化**：独立进程渲染，30fps限制，非聚焦时降为15fps
- **工作流缓存**：常用工作流预编译，执行时跳过解析步骤
- **对话流式输出**：SSE推送，首token延迟<500ms

#### 5.2 隐私增强补充
- **数据加密**：SQLite启用SQLCipher，记忆数据AES-256加密
- **本地备份**：自动加密备份到用户指定目录
- **零外传**：默认关闭所有云API，需用户手动开启并确认

---

## 五、核心难点与解决方案（补充版）

| 难点 | 严重度 | 解决方案 |
|------|--------|----------|
| 桌面端大模型轻量化 | 🔴高 | 支持远程API为主，本地Ollama为辅；按需加载释放；7B量化版 |
| 工作流引擎本地化 | 🔴高 | 前端复用FlowGram画布，后端Python重写轻量引擎（不搬Go微服务） |
| 双引擎记忆融合 | 🟡中 | MemoryBridge统一接口，SQLite统一存储，双写+融合读取 |
| MCP端口冲突 | 🟡中 | 单一MCP网关（localhost:3456），内部路由到不同工具组 |
| 意图识别准确率 | 🟡中 | 规则优先+小模型兜底；混合意图默认双引擎并行 |
| VRM渲染性能 | 🟡中 | 独立BrowserWindow + GPU进程隔离；非聚焦降帧 |
| Hermes自学习闭环适配 | 🟢低 | 保留核心技能创建/改进逻辑，去除多平台网关依赖 |
| Coze前端依赖剥离 | 🟡中 | 逐个替换bot-api调用为本地FastAPI；保留核心画布组件 |
| 跨平台兼容性 | 🟢低 | Electron跨平台API + 平台适配层 |
| 两个Python后端合并 | 🟡中 | FastAPI统一入口，SAP和Hermes作为子模块，按需import |

---

## 六、迭代落地计划（精化版）

| 阶段 | 周期 | 核心目标 | 关键交付物 |
|------|------|----------|-----------|
| **MVP** | 6周 | 验证核心链路 | 基础聊天+本地Hermes调用+简单情感回复+VRM桌宠展示 |
| **V1.0** | 12周 | 办公+情感核心场景 | FlowGram工作流编辑器+10个自定义节点+人设配置+双引擎协同 |
| **V1.5** | 16周 | 工具与记忆完善 | 办公工具集成+情感记忆库+工作流模板市场+语音交互 |
| **V2.0** | 20周 | 体验与生态完善 | 扩展系统开放+数据加密+性能优化+跨平台打包+自动更新 |

### MVP阶段详细里程碑
| 周 | 交付 |
|----|------|
| W1 | 项目脚手架搭建，Electron+React+FastAPI基础框架 |
| W2 | super-agent-party后端集成，VRM桌宠窗口运行 |
| W3 | hermes-agent核心引擎集成，基础对话跑通 |
| W4 | 意图识别器+双引擎路由，办公/情感分流 |
| W5 | 简化UI实现（聊天窗口+伴侣区+VRM） |
| W6 | MVP打包测试，基础使用验证 |

---

## 七、最终价值

### 对用户
- **效率提升**：可视化拖拽编排办公流程，一键执行重复工作
- **情感陪伴**：个性化桌面伴侣，工作不孤单，加班有温度
- **隐私安全**：全程自托管，数据不出本机
- **低门槛**：预置模板+自然语言交互，无需编程

### 对开发者
- **模块化架构**：5层解耦，每层可独立替换升级
- **开放扩展**：复用MCP+Skills+扩展三大协议，生态可扩展
- **技术栈统一**：前端React+后端Python，降低维护成本

---

## 八、技术风险与备选方案

| 风险 | 概率 | 影响 | 备选方案 |
|------|------|------|----------|
| FlowGram剥离云端依赖工作量超预期 | 高 | 工期延误 | 先用简化版画布（React Flow），MVP后再迁移FlowGram |
| 本地Hermes模型性能不足 | 中 | 体验差 | 默认推荐远程API（OpenAI兼容），本地模型作为离线备选 |
| VRM在某些设备渲染卡顿 | 中 | 体验差 | 提供Live2D替代方案（super-agent-party已支持） |
| 双引擎融合输出不自然 | 中 | 体验差 | 增加融合模板库，A/B测试优化 |
| Electron包体积过大(>500MB) | 低 | 下载慢 | 用electron-builder压缩+自动更新增量包 |

---

## 九、目录结构总览（最终版）

```
CompanionOS/
├── electron/                    # Electron主进程
│   ├── main.js                 # 主进程入口
│   ├── preload.js              # 预加载脚本
│   ├── window-manager.js       # 窗口管理器
│   ├── process-manager.js      # 子进程管理
│   └── tray.js                 # 系统托盘
│
├── frontend/                    # React前端
│   ├── src/
│   │   ├── App.tsx             # 主应用（路由+布局）
│   │   ├── views/
│   │   │   ├── HomeView.tsx    # 首页（今日概览）
│   │   │   ├── ChatView.tsx    # 对话模式
│   │   │   ├── FlowView.tsx    # 工作流编辑器
│   │   │   ├── TaskView.tsx    # 任务管理
│   │   │   └── SettingsView.tsx # 设置面板
│   │   ├── components/
│   │   │   ├── Sidebar.tsx     # 侧栏导航
│   │   │   ├── CompanionPanel.tsx # 伴侣区
│   │   │   ├── ChatInput.tsx   # 聊天输入框
│   │   │   └── Statusbar.tsx   # 状态栏
│   │   ├── workflow/           # FlowGram工作流编辑器（从Coze提取）
│   │   │   ├── fabric-canvas/
│   │   │   ├── nodes/          # 扩展节点
│   │   │   │   ├── HermesLLMNode.tsx
│   │   │   │   ├── EmotionalNode.tsx
│   │   │   │   ├── DesktopAutoNode.tsx
│   │   │   │   ├── BrowserNode.tsx
│   │   │   │   └── ...更多节点
│   │   │   ├── sdk/
│   │   │   └── playground/
│   │   └── companion/          # 伴侣区组件
│   │       ├── VRMViewer.tsx   # VRM渲染容器
│   │       ├── EmotionBubble.tsx # 情感气泡
│   │       └── TaskPeek.tsx    # 任务速览
│   └── vite.config.ts
│
├── backend/                     # Python后端（统一FastAPI服务）
│   ├── server.py               # FastAPI入口（端口18080）
│   ├── workflow_engine/        # 工作流执行引擎
│   │   ├── engine.py           # DAG解析+执行
│   │   ├── node_registry.py    # 节点注册表
│   │   └── nodes/              # 节点执行器
│   │       ├── llm_node.py     # Hermes LLM节点
│   │       ├── emotion_node.py # 情感交互节点
│   │       └── tool_node.py    # 工具调用节点
│   ├── agent_bridge/           # 智能体协同总线
│   │   ├── router.py           # 意图识别+路由
│   │   ├── hermes_adapter.py   # Hermes引擎适配
│   │   ├── neuro_adapter.py    # Neuro-sama引擎适配
│   │   └── response_merger.py  # 双引擎结果融合
│   ├── memory_bridge/          # 记忆融合层
│   │   ├── bridge.py           # MemoryBridge统一接口
│   │   ├── work_memory.py      # 工作记忆（Hermes）
│   │   └── emotional_memory.py # 情感记忆（Neuro-sama）
│   └── tools/                  # 本地工具封装
│       ├── office_tools.py     # Office/邮件/日历
│       ├── browser_tools.py    # 浏览器自动化
│       └── system_tools.py     # 系统/文件操作
│
├── agents/                      # AI引擎（子模块/复用代码）
│   ├── hermes/                 # Hermes核心代码（从hermes-agent提取）
│   │   ├── context_engine.py
│   │   ├── adapters/           # 多模型适配器
│   │   ├── skills/             # 技能系统
│   │   └── memory/             # 记忆系统
│   └── neuro/                  # Neuro-sama核心代码（从super-agent-party提取）
│       ├── character_engine.py # 角色卡引擎
│       ├── emotion_engine.py   # 情感引擎
│       ├── vrm_controller.py   # VRM控制器
│       └── voice/              # 语音合成
│
├── vrm/                         # VRM模型资源
│   ├── default/                # 默认模型
│   └── user/                   # 用户自定义模型
│
├── data/                        # 本地数据存储
│   ├── workflows/              # 工作流JSON
│   ├── templates/              # 工作流模板
│   ├── memories/               # 记忆数据（SQLite）
│   ├── profiles/               # 用户/人设配置（YAML）
│   ├── skills/                 # 自学习技能
│   └── logs/                   # 运行日志
│
├── package.json                 # Node.js依赖
├── pyproject.toml              # Python依赖
├── electron-builder.yml        # 打包配置
└── README.md
```

---

*文档版本：v1.0 | 创建时间：2026-04-23 | 作者：CompanionOS架构设计*
